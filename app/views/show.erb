<!DOCTYPE html>
<html>
  <head>
    <title>Task Manager: <%= @task.title %></title>
    <link type="text/css" rel="stylesheet" href="/main.css" />
  </head>
  <body>
    <a href="/tasks">Task Index</a>
    <h1 class="task_title"><%= @task.title %></h1>
    <p><%= @task.description %></h1>
    <form action="/tasks/remove/<%= @task.id %>" method="post">
      <input type="submit" value="Remove Task" />
    </form>
  </body>
</html>
